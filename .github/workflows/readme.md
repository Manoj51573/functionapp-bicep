# Setting up Github actions

## Build agent

### Prerequisites

1. Opt for linux (Ubuntu) agent over Windows, as Docker Desktop may require a paid subscription whereas Linux uses Docker CE and as of this edit, is still free for use.
1. .NET v8 Runtime
1. Git
1. Azure CLI
1. Docker CE (Linux)/Docker Desktop (Windows)
